import java.util.Scanner;

public class test {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        // 1)Write a program to add 8 to the number x and then divide it by 3. Now, the
        // modulus of the quotient is taken with 5 and then multiply the resultant value
        // by 5. Display the final result.

        // int x = sc.nextInt();
        // int y = (((x + 8) / 3) % 5) * 5;
        // System.out.println(y);

        // 2)Swap two numbers without the use of third variable

        // int a = sc.nextInt();
        // int b = sc.nextInt();
        // a=a+b;
        // b=a-b;
        // a=a-b;
        // System.out.println(a+" "+b);

        // 3)Write a program to calculate the sum of the digits of a 3-digit number.

        // int a = sc.nextInt();
        // int rem = 0;
        // while (a > 0) {
        // rem = rem + a % 10;
        // a = a / 10;
        // }
        // System.out.println(rem);

        // 4)Assign values of variables 'a' and 'b' as 55 and 70 respectively and then
        // check if

        // int a = sc.nextInt();
        // int b = sc.nextInt();
        // System.out.println(a<50 && a<b);
        // System.out.println(a<50 || a<b);

        // 5)Find the total number of bits needed to be flipped to convert x to y

        // int count = 0;
        // int x = sc.nextInt();
        // int y = sc.nextInt();
        // int n = x ^ y;
        // System.out.println(n);
        // while (n != 0) {
        // n = n & (n - 1);
        // count++;
        // }
        // System.out.println(count);
    }
}
