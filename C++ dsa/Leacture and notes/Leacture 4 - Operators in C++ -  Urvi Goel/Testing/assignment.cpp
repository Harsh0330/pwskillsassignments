#include <iostream>
using namespace std;
int main()
{
    // 1)Write a program to check whether two numbers (entered by user) are equal or not (0 for not equal, 1for equal)

    // int x, y;
    // cin >> x;
    // cin >> y;
    // cout << (x == y) ? 1 : 0;

    // 2)Write a program to take the values of two variables a and b from the keyboard and then check if both theconditions 'a < 50' and 'a < b' are true.

    // int a, b;
    // cin >> a;
    // cin >> b;
    // cout << ((a < 50) && (a < b)) ? 1 : 0;

    // 3)There are 45 total pupils in the class, 25 of whom are boys. Write a program to find how many girlsreceived grades "A" if 17 boys made up 80% of the students that received grades "A"

    // int total, boys, girls;
    // boys = 17;
    // total = (80 * 45) / 100;
    // girls = total - boys;
    // cout << "Number of girls getting grade A = " << girls << endl;

    // 4)Write a program to calculate the sum of the first and the second last digit of a 5 digit number.

    // int n, first, second, third, fourth, fifth, sum;
    // n = 12345;
    // first = n / 10000;
    // n = n % 10000;
    // second = n / 1000;
    // n = n % 1000;
    // third = n / 100;
    // n = n % 100;
    // fourth = n / 10;
    // fifth = n % 10;
    // sum = first + fourth;
    // cout << "sum of first and fourth digit is: " << sum << endl;

    // 5) write a program to calculate the sum of digits of a 3 digit number

    // int n, first, second, third, sum;
    // n = 524;
    // first = n / 100;
    // n = n % 100;
    // second = n / 10;
    // third = n % 10;
    // sum = first + second + third;
    // cout << "sum of 3 digits is: " << sum << endl;

    // 6)Design a calculator to perform basic arithmetic operations (+,-,/,*)
    // float num1, num2;
    // cout << "Enter the first number ";
    // cin >> num1;
    // cout << "Enter the second number ";
    // cin >> num2;
    // cout << "Sum = " << num1 + num2 << endl;
    // cout << "Difference = " << num1 - num2 << endl;
    // cout << "Product = " << num1 * num2 << endl;
    // cout << "Division = " << num1 / num2 << endl;
}