#include <iostream>
using namespace std;
int main()
{
    // 1) Take 2 integer values in two variables x and y and print their product

    // int a, b;
    // cin >> a >> b;
    // cout << a * b<<endl;

    // 2)Print the ASCII value of character ‘U’.

    // cout<<(int)'U';

    // 3)Write a C++ program to take length and breadth of a rectangle and print its area

    // int a, b;
    // cin >> a >> b;
    // cout << a * b << endl;

    // 4)Write a C++ program to calculate the cube of a number

    // int a;
    // cin >> a;
    // cout << a*a*a << endl;
    // return 0;

    // 5)Write a C++ program to find size of basic data types

    // cout << "\nSize of fundamental data types :\n";
    // cout << " The sizeof(char) : " << sizeof(char) << " bytes \n";
    // cout << " The sizeof(short) : " << sizeof(short) << " bytes \n";
    // cout << " The sizeof(int) : " << sizeof(int) << " bytes \n";
    // cout << " The sizeof(long): " << sizeof(long) << " bytes \n";
    // cout << " The sizeof(long long) :" << sizeof(long long) << " bytes \n";
    // cout << " The sizeof(float) :" << sizeof(float) << " bytes \n";
    // cout << " The sizeof(double) :" << sizeof(double) << " bytes \n";
    // cout << " The sizeof(long double) :" << sizeof(long double) << " bytes \n";
    // cout << " The sizeof(bool) : " << sizeof(bool) << " bytes \n\n";

    // 6)Write a C++ program to swap two numbers with the help of a third variable

    // int a, b;
    // cin >> a >> b;
    // int c=a;
    // a=b;
    // b=c;
    // cout << a << " " << b << endl;
}